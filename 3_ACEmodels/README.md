Download the models from the Figshare link: 10.6084/m9.figshare.30577157
- ACE model generated for each iteration is supplied as separate directory
- Use the `4_relaxation_JuLIP/save_traj.jl` script to load these models and perform structural relaxation for a vacancy at each site in a Pt-Au alloy GB structure.
